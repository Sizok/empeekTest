(function () {
    function cropImageService() {
        var factory = {};
        

        return factory;
    }

    cropImageService.$inject = [];
    angular.module('cropImage.Service', []).factory('cropImageService', cropImageService);
})();
